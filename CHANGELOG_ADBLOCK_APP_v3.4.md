# v3.4 App-Safe

- Menambah profil `app-safe` di pipeline.
- Mewarisi seluruh proteksi `child-safe`.
- Menambah 76 target iklan aplikasi yang spesifik.
- Menambah satu inline provider `app-ad-safe` untuk OpenClash/OpenWrt.
- Android tetap tanpa provider MRS tambahan dan menggunakan rules langsung.
- Tidak memblokir `googlevideo.com`, `spotifycdn.com`, `akamaized.net`, `microsoft.com`, `apple.com`, `xiaomi.com`, atau `heytapmobile.com` secara luas.
- Turtlecute tetap 128/128 pada benchmark host yang dipakai paket.
